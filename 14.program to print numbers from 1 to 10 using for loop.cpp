#include <iostream>

int main() {
    // Use a for loop to print numbers from 1 to 10
    for (int i = 1; i <= 10; ++i) {
        std::cout << i << " ";
    }

    // Add a newline at the end for better formatting
    std::cout << std::endl;

    return 0;
}
